
// Farklı veri türlerinde değişken oluşturma
let ad;// undefined
let fullName='Efsane';// string
let isStudent = false; // boolean
let number= 25; // number
let nullValue = null; // null
const euler = 2.71828; // number (const)


console.log(  isStudent,fullName,ad,number,nullValue,euler);

console.log(
    typeof isStudent,
    typeof fullName,
    typeof ad,
    typeof number,
    typeof nullValue,
    typeof euler

)

//Değer atamadan sadece değişken oluşturma işlemine değişken deklarasyonu denmektedir.
// Bir değişkene ise ilk değerin atanması Bir değişkene ilk değeri atama işlemine değişken başlatma olarak ifade edilir.

let  declaredVariable;
 declaredVariable = 'Efsane';

let declaredVariableTwo;
declaredVariableTwo = 'Deneme';

let declaredVariableThree;
declaredVariableThree = 'declaredVariableThree';

let declaredVariableFour;
declaredVariableFour = 'declaredVariableFour';


//
myAge = 25;
yourAge = 30;

 console.log(`Ben  ${myAge} yaşındayım`,`Sen  ${yourAge} yaşındasın`);
